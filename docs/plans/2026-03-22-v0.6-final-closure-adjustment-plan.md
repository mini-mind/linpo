# v0.6 Reconciliation and Closure Adjustment Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在已完成 v0.6 主体实现与验收的基础上，继续完成最终对账、范围收敛与发布前清理，确保 active plan、PRD、architecture、README、实现与证据长期一致。

**Architecture:** 当前代码与主要验证门已经成立：overview/topology/kanban/session 的 IA、instanceId 贯通、聚合 diagnostics/freshness/request_id、Playwright E2E、质量门与健康检查都已通过。本计划只处理“最终收口”问题：文档/证据持续维护、遗留控制面边界决策、脏工作区整理、必要时补充审计与提交切分。

**Tech Stack:** Python/FastAPI, TypeScript/React/Vite, Vitest, Playwright, Markdown docs, git

---

### Task 1: 固化最终对账清单到 notepad

**Files:**
- Modify: `.sisyphus/notepads/v0.6-execution-work-plan/issues.md`
- Modify: `.sisyphus/notepads/v0.6-execution-work-plan/decisions.md`
- Modify: `.sisyphus/notepads/v0.6-execution-work-plan/learnings.md`

**Step 1: 追加当前真实结论**

把以下结论 append 到 notepad：
- 文档真源已补入 `request_id / freshness / diagnostics / error envelope` 语义
- Task 10 已有精确命名证据文件 `task-10-release.txt` 与 `task-10-release-error.txt`
- fresh 验证命令已再次通过：backend alignment / frontend tests / make quality / health / e2e
- F4 remaining concern 仍是 repo 级既存 control surface，而不是本轮新增工作

**Step 2: 记录仍待产品/架构裁决的问题**

在 `issues.md` 明确区分：
- `已解决`：Task 8 文档语义缺口、Task 10 evidence naming gap
- `未解决但已隔离`：repo 中历史遗留 runtime write/control surface 是否必须在 v0.6 发布前物理删除

**Step 3: 运行最小一致性检查**

Run:
- `git diff -- .sisyphus/notepads/v0.6-execution-work-plan`

Expected:
- 仅出现 append-only 变更，无覆盖式重写

**Step 4: Commit**

```bash
git add .sisyphus/notepads/v0.6-execution-work-plan/issues.md .sisyphus/notepads/v0.6-execution-work-plan/decisions.md .sisyphus/notepads/v0.6-execution-work-plan/learnings.md
git commit -m "docs: 同步 v0.6 最终对账记录"
```

---

### Task 2: 决定 v0.6 对既有控制面的处理策略

**Files:**
- Read: `app/api/agents.py`
- Read: `frontend/src/api/client.ts`
- Read: `frontend/src/api/types.ts`
- Read: `app/services/openclaw_client.py`
- Modify: `docs/prd/2026-03-15-linpo-v0.1-observer-prd.md` (only if strategy needs explicit wording)
- Modify: `docs/architecture/2026-03-15-observer-architecture.md` (only if strategy needs explicit wording)

**Step 1: 列出既有控制面残留清单**

明确以下能力是否仍在主实现中可达：
- send / abort / patch / reset / delete
- operator.write / pause 等 runtime controls

**Step 2: 在两种策略中二选一**

策略 A：**物理收口**
- 移除或禁用 v0.6 主路径不应保留的 runtime write surface
- 适用于把 F4 从 PARTIAL 拉到 APPROVE

策略 B：**文档声明隔离**
- 明确这些是历史 capability / future path，不属于 v0.6 IA 主路径与验收范围
- 适用于当前阶段不做大手术，但必须让 PRD/architecture/plan 口径一致

**Step 3: 如选策略 A，先写失败测试再最小实现**

Examples:
- UI path 不应渲染 destructive/runtime controls
- 非 v0.6 验收路径不得被新入口引用

**Step 4: 如选策略 B，补强文档措辞**

明确：
- repo 保留历史控制能力实现不等于 v0.6 主路径开放这些能力
- v0.6 验收只以 overview/topology/kanban/session observer path 为准

**Step 5: 验证**

Run:
- `rg --line-number "pause|reset|delete|send|operator.write" app frontend docs`

Expected:
- 若策略 A：主路径引用显著减少或消失
- 若策略 B：docs 中出现更明确的边界说明，并且主导航/主路径未引用这些能力

---

### Task 3: 整理 Task 10 浏览器证据的长期可追溯性

**Files:**
- Modify: `frontend/playwright.config.ts` (only if deciding to retain artifacts on success)
- Modify: `.sisyphus/evidence/task-10-release.txt`
- Modify: `.sisyphus/evidence/task-10-release-error.txt`
- Optional: create artifact index in `.sisyphus/evidence/`

**Step 1: 决定是否保留成功运行 trace/screenshot/video**

Option A (recommended):
- 将 Playwright config 改为在成功运行也保留 trace 或 screenshot，增强长期审计性

Option B:
- 继续沿用 Task 9 截图 + Task 10 文本索引方式，只要计划接受这种封装

**Step 2: 如果采用 A，做最小配置变更**

Example:
- `trace: "on"` or `retain-on-failure` -> `on-first-retry`/`on`
- `screenshot: "only-on-failure"` -> `on`

**Step 3: 重新运行 Task 10 验证命令**

Run:
- `make quality`
- `curl -i http://175.178.213.10:8000/health`
- `PLAYWRIGHT_BASE_URL=http://175.178.213.10:5173 npm --prefix frontend run e2e -- --grep "v0.6"`

Expected:
- 全部通过
- 成功产物可用于长期审计

**Step 4: 更新证据索引**

在 `task-10-release.txt` 里明确列出：
- 命令
- base URL
- host
- 对应 screenshot / trace / log 位置

---

### Task 4: 清理主工作区脏状态并切分提交边界

**Files:**
- Inspect current dirty files
- Commit only scoped files per topic

**Step 1: 分类当前 dirty/untracked 文件**

Group into:
- docs truth-source updates
- evidence files
- notepad audit trail
- code changes already intended for v0.6
- unrelated leftovers (if any)

**Step 2: 对每组做最小验证**

Run:
- `git diff --name-only`
- `git diff --stat`
- `git diff -- <scoped files>`

Expected:
- 每次提交边界清晰，不混入无关文件

**Step 3: 按主题原子提交**

Recommended commit sequence:
1. docs truth-source alignment
2. evidence closure
3. optional scope-fidelity resolution
4. notepad / audit record sync

---

### Task 5: 重新执行 Final Reconciliation Gate

**Files:**
- Read: `.sisyphus/plans/v0.6-execution-work-plan.md`
- Read: `README.md`
- Read: `docs/prd/2026-03-15-linpo-v0.1-observer-prd.md`
- Read: `docs/architecture/2026-03-15-observer-architecture.md`
- Read: `.sisyphus/evidence/*`

**Step 1: 逐条检查 Must Have / Must NOT Have / Acceptance Criteria / Success Criteria**

Create explicit checklist for:
- default landing
- three-page drill-down
- kanban scope restraint
- markdown + tool-call summary
- request_id / freshness / diagnostics consistency
- instanceId constraint
- ravin/deployed evidence traceability

**Step 2: 重新判定 R1-R4 与 F1-F4**

Use fresh evidence only.

**Step 3: 写最终结论**

Expected format:
- R1: PASS/PARTIAL/REJECT + evidence
- R2: PASS/PARTIAL/REJECT + evidence
- R3: PASS/PARTIAL/REJECT + evidence
- R4: PASS/PARTIAL/REJECT + evidence
- F1/F2/F3/F4 each with exact blockers or approval basis

---

### Task 6: 若仍未完全收口，输出下一轮执行计划

**Files:**
- Create: `docs/plans/2026-03-22-v0.6-final-closure-adjustment-plan.md` (this file)
- Optional Create/Modify: follow-up plan doc for unresolved blocker

**Step 1: 区分“必须本轮解决”与“可转下轮”**

Must-fix-now examples:
- 计划硬门禁缺失证据
- 文档真源与实现矛盾

Can-defer examples (only with explicit justification):
- 历史 capability 保留但不在 v0.6 主路径暴露，且文档已明确隔离

**Step 2: 写出下轮最小任务集**

每项都要包含：
- exact files
- verification command
- done condition

**Step 3: 验证计划可执行**

Run:
- `rg --line-number "Task 1:|Task 2:|Task 3:" docs/plans/2026-03-22-v0.6-final-closure-adjustment-plan.md`

Expected:
- plan contains actionable step-by-step tasks, not vague prose
