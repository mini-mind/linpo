- 2026-03-21: 当前工作区的 v0.6 active plan 曾引用不存在的 `2026-03-21` docs 路径；后续必须以主工作区现存且已收敛的 `README.md`、`docs/prd/2026-03-15-linpo-v0.1-observer-prd.md`、`docs/architecture/2026-03-15-observer-architecture.md` 作为文档真源，不再依赖虚空引用。
- 2026-03-21: worktree 中关于 URL 真源、动作环四词、无额外 prompt、remembered state 只作记忆层的约束，必须系统吸收到 v0.6 active plan，而不能只留在历史 notepads 中。
- 2026-03-22: Playwright `getByText` / `getByRole` 在 overview 与 topology 页面容易命中重复文案（如 `全部 agents`、实例名、`进入实例 ...`）；E2E 断言需要尽量加 `exact`、`first()` 或切到 heading / aria-label 语义以避免 strict mode 失败。
- 2026-03-22: Task 10 首轮 `make quality` 失败并非实现回归，而是 `tests/test_observer_realtime_state.py` 仍在重置旧的 `_OPENCLAW_DATA_SOURCE` / `_OPENCLAW_DATA_SOURCE_CONFIG`；当缓存实现切换为 `_OPENCLAW_DATA_SOURCES` 后，这类测试会出现假阴性。
- 2026-03-22: F4 范围忠实性深审 Verdict = REJECT。重点复核 Task 8/9 落盘文件后，未见它们新增模板系统 / 工作流平台化 / 额外 prompt 注入；但仓库仍保留控制面写接口与 `operator` 写能力残留，严格口径下不满足“实现没有越界到被明确排除能力”。高风险影响面：`app/api/agents.py`、`frontend/src/api/client.ts`、`frontend/src/api/types.ts`、`app/services/openclaw_client.py`。Task 10 无新增仓库代码文件可审，本轮按 plan 要求核对已落盘实现入口。
  
  | 约束 | 证据 | 结论 |
  | --- | --- | --- |
  | 不实现模板系统 / 模板选择器 / 动态多视图平台能力 | `grep(template|workflow|模板系统|工作流)` 在 `frontend/src` 与 `app/` 下无命中；复核 `app/api/aggregate.py:50`、`app/api/aggregate.py:71`、`frontend/src/components/OverviewPage.tsx:18`、`frontend/src/components/InstanceTopology.tsx:127` 仅为聚合读入口。 | APPROVE |
  | 不做工作流平台化改造 | `grep(workflow|工作流|审批|治理|项目管理)` 在 `frontend/src/components` 与 `app/` 下无命中；`frontend/src/components/OverviewPage.tsx:60`、`frontend/src/components/InstanceTopology.tsx:278` 均维持观测/关系语义。 | APPROVE |
  | 不通过静默额外 prompt 制造首页汇报感 | `grep(prompt)` 在 `app/` 与 `frontend/src` 下无命中；`frontend/src/components/OverviewPage.tsx:18-23` 只调用 `getAggregateOverview()`，`frontend/src/components/InstanceTopology.tsx:127-145` 只读聚合拓扑和实例配置。 | APPROVE |
  | 不把 overview 改成告警中心 / 审计台 / 工作流中心 | `frontend/src/components/OverviewPage.tsx:60-99` 展示全部 agents、freshness、request_id、诊断摘要；`frontend/src/components/OverviewPage.tsx:165-177` 只保留 agent drill-down 与去 `/topology` 导航。 | APPROVE |
  | 不用假数据掩盖真实失败 | `frontend/src/components/OverviewPage.tsx:94-149`、`frontend/src/components/InstanceTopology.tsx:338-358` 显式展示 diagnostics / error / recoverable / next_step；`frontend/e2e/v0.6-overview-topology-session.spec.ts:169-341` 专门验降级态可见。 | APPROVE |
  | 不在 topology / session 主路径暴露 `pause/reset/send/delete` destructive/runtime controls | `frontend/src/components/InstanceTopology.tsx:280-390` 的动作环固定为 `查看 / 进入 / 配置 / 关系`；`frontend/src/components/SessionActions.tsx:21-29` 明示“只读观察”；`frontend/src/components/AgentWorkspace.readonly.test.tsx:81-103` 断言无暂停 / 重置 / 删除 / 发送按钮。 | APPROVE（仅限 UI 主路径） |
  | 不新增写操作主线 / 不把 `OperatorAdapter` 变成 v0.6 主适配器 | `app/api/aggregate.py:50-87` 为只读聚合接口，Task 8 本身未新增写主线；但 `app/api/agents.py:313-401` 仍实现 `chat.send` 与 `chat.abort -> pause`，`app/api/agents.py:567-652` 仍实现 `sessions.patch/reset/delete`，`frontend/src/api/client.ts:208-288` 仍导出对应写调用，`frontend/src/api/types.ts:169-207` 仍保留控制/写请求类型，`app/services/openclaw_client.py:170-223` 仍以 `role="operator"` + `operator.*` scopes 建立写能力连接。 | REJECT；严重性=高；影响面=后端可调用控制接口 + 前端共享 client/types 仍保留控制语义，observer-only 边界仍可被绕开 |
  | 不做控制面扩张 | 同上：`app/api/agents.py:313-401`、`app/api/agents.py:567-652`、`frontend/src/api/client.ts:208-288`、`app/services/openclaw_client.py:170-223` 证明 runtime write/control surface 仍在仓库主实现中，不只是测试桩。 | REJECT；严重性=高；影响面=运行时控制面仍具备真实实现，不是纯文档遗留 |
  | 不在消息历史中直接暴露原始工具调用 JSON | `app/api/schemas.py:79-87` 预览项已压平成 `{role, text}`；`app/api/agents.py:541-556` 预览接口只回传 `role/text`；`frontend/src/components/AgentWorkspace.tsx:764-775` 只渲染角色 + 文本，无 JSON dump 入口。 | APPROVE |
  | Task 8/9/10 是否引入新增越界行为 | Task 8 对应 `app/api/aggregate.py`、`app/api/schemas.py`、`frontend/src/components/OverviewPage.tsx`、`frontend/src/components/InstanceTopology.tsx` 均为读模型/诊断展示；Task 9 对应 `frontend/playwright.config.ts:1-21` 与 `frontend/e2e/v0.6-overview-topology-session.spec.ts:109-341` 仅验证 overview/topology/session 与降级态；Task 10 本轮无仓库代码文件可审。 | Task 8/9 未新增越界；REJECT 原因来自仓库既存控制面残留未被收口 |
- 2026-03-22: F1 Plan Compliance Audit（oracle）结论 = REJECT。
  - Task 8 (`.sisyphus/plans/v0.6-execution-work-plan.md:407`)：PARTIAL。代码与当前验收命令已满足核心契约对齐：后端 `app/api/schemas.py:179`、`app/api/schemas.py:184`、`app/api/schemas.py:215`、`app/api/schemas.py:251` 与前端 `frontend/src/api/types.ts:48`、`frontend/src/api/types.ts:72`、`frontend/src/api/types.ts:103`、`frontend/src/api/types.ts:155` 使用同名 `freshness` / `request_id` / `error` 字段；展示层 `frontend/src/components/OverviewPage.tsx:84`、`frontend/src/components/OverviewPage.tsx:102`、`frontend/src/components/InstanceTopology.tsx:283`、`frontend/src/components/InstanceTopology.tsx:338` 已把 freshness/diagnostics/error envelope 暴露为 summary/badge。fresh evidence：`.venv/bin/pytest tests/integration/test_aggregate_api.py tests/integration/test_agents_api.py tests/test_observer_realtime_state.py -q` => `66 passed, 2 skipped`；`npm --prefix frontend run test -- src/components/OverviewPage.test.tsx src/components/InstanceTopology.test.tsx src/api/realtimeClient.test.ts` => `18 passed`。缺口：计划要求的 `.sisyphus/evidence/task-8-contract-alignment*.txt` 不存在。最小补齐动作：把两条已通过命令的输出补录到 Task 8 证据文件。
  - Task 9 (`.sisyphus/plans/v0.6-execution-work-plan.md:448`)：PARTIAL。实现面已落地：`frontend/package.json:6` 提供 `e2e` 脚本并声明 `@playwright/test`；`frontend/playwright.config.ts:3`-`frontend/playwright.config.ts:19` 固定 `PLAYWRIGHT_BASE_URL` 默认值与 Playwright 入口；`frontend/e2e/v0.6-overview-topology-session.spec.ts:109`-`frontend/e2e/v0.6-overview-topology-session.spec.ts:167` 覆盖注册/登录、默认 `/overview`、overview/topology/session drill-down；`frontend/e2e/v0.6-overview-topology-session.spec.ts:169`-`frontend/e2e/v0.6-overview-topology-session.spec.ts:341` 覆盖降级态可见。fresh evidence：`npm --prefix frontend run test -- src/components/LoginPage.test.tsx src/components/OverviewPage.test.tsx src/components/InstanceTopology.test.tsx src/components/SessionPage.test.tsx` => `26 passed`；`PLAYWRIGHT_BASE_URL=http://175.178.213.10:5173 npm --prefix frontend run e2e -- --grep "v0.6"` => `2 passed`。缺口：计划要求的 `.sisyphus/evidence/task-9-playwright.png` / `.sisyphus/evidence/task-9-playwright-error.png` 不存在。最小补齐动作：从 ravin 重新执行同一 spec，并把 happy/degraded 两条浏览器证据按计划路径落盘。
  - Task 10 (`.sisyphus/plans/v0.6-execution-work-plan.md:489`)：PARTIAL。三条验收命令当前均通过：`make quality` => `142 passed, 2 skipped` + `0 errors, 0 warnings, 0 notes` + frontend build success；`curl -i http://175.178.213.10:8000/health` => `HTTP/1.1 200 OK` / `{"status":"ok"}`；`PLAYWRIGHT_BASE_URL=http://175.178.213.10:5173 npm --prefix frontend run e2e -- --grep "v0.6"` => `2 passed`。现有日志证据位于 `.sisyphus/evidence/task-10-release-20260322-024344-quality.txt:1`、`.sisyphus/evidence/task-10-release-20260322-024344-health.txt:6`、`.sisyphus/evidence/task-10-release-20260322-024344-e2e.txt:6`。缺口：计划 acceptance 明确要求“从 ravin 发起”并保留 screenshot/trace/log 证据，但当前仓库仅看到文本日志，`frontend/playwright-report/**` 为空、`frontend/test-results/**` 仅有 `.last-run.json`，无法证明成功运行保留了 screenshot/trace，也无法证明命令来源是 ravin。最小补齐动作：从 ravin 再跑 Task 10 三命令，并保存带主机来源的 screenshot/trace/log 证据。
  - Success Criteria (`.sisyphus/plans/v0.6-execution-work-plan.md:548`)：PARTIAL。`docs/`/backend/frontend 关于 overview/topology/session + diagnostics/freshness/request_id 的语义当前一致（`README.md:15`-`README.md:35`，`docs/prd/2026-03-15-linpo-v0.1-observer-prd.md:204`-`docs/prd/2026-03-15-linpo-v0.1-observer-prd.md:227`，`docs/architecture/2026-03-15-observer-architecture.md:47`-`docs/architecture/2026-03-15-observer-architecture.md:63`，`app/api/schemas.py:179`-`app/api/schemas.py:260`，`frontend/src/api/types.ts:48`-`frontend/src/api/types.ts:165`）；overview/topology 已统一进入 canonical session route（`frontend/src/components/SessionPage.tsx:14`-`frontend/src/components/SessionPage.tsx:20`，`frontend/src/components/OverviewPage.tsx:201`-`frontend/src/components/OverviewPage.tsx:207`，`frontend/src/components/InstanceTopology.tsx:369`-`frontend/src/components/InstanceTopology.tsx:373`，`frontend/src/components/InstanceTopology.tsx:431`-`frontend/src/components/InstanceTopology.tsx:435`，加上 `npm --prefix frontend run test -- src/components/SessionPage.test.tsx` 已在组合测试中通过）；`instanceId` 已约束 HTTP/WS/session 读取（`frontend/src/api/client.ts:31`-`frontend/src/api/client.ts:44`，`frontend/src/api/realtimeClient.ts:53`-`frontend/src/api/realtimeClient.ts:66`，`frontend/src/components/SessionPage.tsx:62`-`frontend/src/components/SessionPage.tsx:76`，`npm --prefix frontend run test -- src/api/client.test.ts src/api/realtimeClient.test.ts src/components/SessionPage.test.tsx` => `21 passed`）。但“部署后的目标环境通过 ravin Playwright 验收，且存在可追溯证据文件”这一条仍缺 ravin 来源与 screenshot/trace 证据，所以 Success Criteria 只能判为 PARTIAL。最小补齐动作：按 Task 9/10 缺口补齐 ravin 浏览器证据后重新审计。
- 2026-03-22: F2 Code Quality Review 结论 = REJECT。
  - Summary：审查范围聚焦提交 `0724f7b`、`3501659`、`94b3104` 以及 Task 8/9/10 计划门禁。实际检查命令：`git log --oneline -10`、`git show --name-only --oneline 0724f7b 3501659 94b3104`、`git show --stat --patch 0724f7b`、`git show --stat --patch 3501659`、`git show --stat --patch 94b3104`、`/data/projects/linpo/.venv/bin/pytest tests/integration/test_aggregate_api.py tests/integration/test_agents_api.py tests/test_observer_realtime_state.py -q`（`66 passed, 2 skipped`）、`npm --prefix frontend run test -- src/components/OverviewPage.test.tsx src/components/InstanceTopology.test.tsx src/api/realtimeClient.test.ts src/components/LoginPage.test.tsx src/components/SessionPage.test.tsx`（`5 files, 36 tests passed`）、`npm --prefix frontend run e2e -- --list --grep "v0.6"`（列出 2 条 Playwright 用例）。
  - Blocking：Task 8 未真正完成前后端失败态统一。plan 要求 `overview/topology` 在未授权、失败、部分成功场景都统一表达 `code/message/request_id/recoverable/next_step`，且字段不能漂移（`.sisyphus/plans/v0.6-execution-work-plan.md:409`-`.sisyphus/plans/v0.6-execution-work-plan.md:410`）。后端 envelope 已落地（`app/api/aggregate.py:28`，`tests/integration/test_aggregate_api.py:192`），但前端 API 层在非 2xx 时直接丢弃响应体并抛通用错误，页面只显示字符串错误，未消费 envelope 字段（`frontend/src/api/client.ts:60`-`frontend/src/api/client.ts:75`，`frontend/src/components/OverviewPage.tsx:18`-`frontend/src/components/OverviewPage.tsx:29`，`frontend/src/components/InstanceTopology.tsx:127`-`frontend/src/components/InstanceTopology.tsx:139`）。现有前端测试只覆盖 `200 + diagnostics` 的 partial-failure，没有覆盖真正的 401/failed envelope 消费（`frontend/src/components/OverviewPage.test.tsx:180`，`frontend/src/components/InstanceTopology.test.tsx:211`）。
  - Blocking：Task 9 Playwright 覆盖不足，缺少 plan 明确要求的 `kanban` 与“三页都能进入 `/session/:instanceId/:agentId`”门禁（`.sisyphus/plans/v0.6-execution-work-plan.md:57`，`.sisyphus/plans/v0.6-execution-work-plan.md:450`）。当前 spec 只访问 `/overview` 与 `/topology`，没有任何 `/kanban` 访问、断言或从 kanban 进入会话的验证（`frontend/e2e/v0.6-overview-topology-session.spec.ts:124`-`frontend/e2e/v0.6-overview-topology-session.spec.ts:167`，`frontend/e2e/v0.6-overview-topology-session.spec.ts:319`-`frontend/e2e/v0.6-overview-topology-session.spec.ts:341`）。
  - Blocking：Task 9 的降级场景依赖 `page.route()` mock，不满足“不能写成只适用于本地 mock、需针对真实 stale/unreachable 环境验收”的计划约束（`.sisyphus/plans/v0.6-execution-work-plan.md:451`，`.sisyphus/plans/v0.6-execution-work-plan.md:480`-`.sisyphus/plans/v0.6-execution-work-plan.md:484`）。提交里直接构造 `degradedOverview` / `degradedTopology` 并拦截 `/aggregate/overview`、`/aggregate/topology`、`/instances`（`frontend/e2e/v0.6-overview-topology-session.spec.ts:176`-`frontend/e2e/v0.6-overview-topology-session.spec.ts:317`），这只能证明静态渲染，不足以证明已部署链路中的真实 freshness/diagnostics/request_id 联动成立。
  - Non-blocking：`frontend/playwright.config.ts:15`-`frontend/playwright.config.ts:20` 只在失败时保留 `trace/screenshot/video`，与 Task 9/10 强调的“成功验收需有可追溯浏览器证据”不完全对齐；这不是当前最直接的阻断，但会继续削弱验收证据的可追溯性。
  - Non-blocking：Task 10 对应修复提交 `94b3104` 是最小且合理的测试修复。它只把测试里的 monkeypatch 目标从旧缓存名切到当前实现使用的 `_OPENCLAW_DATA_SOURCES`（`tests/test_observer_realtime_state.py:999`-`tests/test_observer_realtime_state.py:1027`），与实际实现一致（`app/services/observer_data.py:1058`-`app/services/observer_data.py:1095`），未引入额外作用域污染。
  - Verdict：REJECT。阻断项集中在 Task 8 失败态 envelope 未端到端贯通，以及 Task 9 Playwright 覆盖/真实性未达到计划门禁；Task 10 的单独修复提交本身通过审查，但不足以扭转整体结论。
- 2026-03-22: Task 8 gap fix：前端 aggregate 读取在非 2xx 时开始消费后端 `error` 包络，`ApiError` 现携带 `envelope`，`OverviewPage` / `InstanceTopology` 错误态会显示 `code`、`request_id`、`recoverable` 与 `next_step`；回归测试新增了 401/503 非 2xx 场景断言。对应证据已按计划命名落盘到 `.sisyphus/evidence/task-8-contract-alignment.txt` 与 `.sisyphus/evidence/task-8-contract-alignment-error.txt`。注意：根据上层工作约束，本次未修改 `.sisyphus/plans/v0.6-execution-work-plan.md` 复选框，避免违反 plan read-only 规则。
- 2026-03-22: Task 9 gap close：将 `/kanban` 正式接入主导航与聚合工作信号页，复用 aggregate overview 数据模型补齐 overview / topology / kanban 三页统一进入 canonical `/session/:instanceId/:agentId` 的浏览器级覆盖；本轮 happy / degraded 证据已落盘到 `.sisyphus/evidence/task-9-playwright.png` 与 `.sisyphus/evidence/task-9-playwright-error.png`。
