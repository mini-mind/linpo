# Linpo 治理与协作规则（唯一真源）

## 必须遵守

- 除非任务过于简单，否则执行流程必须遵循：调查 -> 设计（边界/输入输出/验收标准） -> 实现 -> 验证 -> 收敛。
- 与用户沟通使用中文。
- 关键代码要用人话写明中文注释，强调可读性与解释性。
- 任务委派给 subagents（`gpt-5.3-codex`） 并行推进，主agent负责任务拆解、subagents调度、和验收成果。

## 工程准则

- BUG/疑难问题默认红绿测试（先红后绿）。
- 简单小改动可不新增测试，但必须做最小验证并说明。
- 失败处理默认“可回滚即回滚、错误默认暴露”；仅业务明确要求时才做降级兜底。
- 所有 Python 相关命令（pytest、pyright 等）需在激活 `.venv` 环境后执行。
- 新目录/新模块/新技术栈：先更新文档再落地。同一约束只在一个文档定义，避免并行口径。过时文档直接删除，不保留废弃占位。

## 禁止事项

1. 禁止吞异常（如 `except Exception: pass`）。
2. 禁止未经用户明确要求主动提交（commit）。
3. 禁止未经用户同意的托底策略和回退策略。
4. 禁止频繁催促 subagents。
5. 禁止以“过渡层/兼容壳/兜底别名”维持旧行为。

## 验证与验收

- 后端核心逻辑变更必须补单元测试。
- BUG/疑难问题需提供红绿测试证据。
- 浏览器验收由 `ravin` 发起 Playwright（`ravin` 非构建/部署主机）。