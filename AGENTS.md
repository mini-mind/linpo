# 灵盘（Linpo）

## 概览
灵盘（Linpo）是一个从小闭环起步、逐步扩张的平台型项目。当前有效产品方向是**面向人类的多实例 agent 工作与协作入口**：用户登录后通过 `overview / topology / kanban / team / session` 五页 IA 理解总体运行态势、结构关系、任务推进、团队入口与会话工作区。

当前仓库已完成一次项目级重置：旧版 roboard 的实现、文档与配置已移出当前仓库根目录，仅作为仓库外本地归档参考，不再作为当前项目权威来源。

开发阶段对外域名暂定为 `linpo.duckdns.org`。

## 目录结构
```
./
├── README.md
├── AGENTS.md
└── docs/
    ├── prd/
    ├── architecture/
    └── plans/
```

| 仓库根目录 | `./` | 灵盘（Linpo）当前开发工作根目录 |
| 文档规范 | `docs/` | 产品、架构与计划文档 |
| 仓库治理 | `AGENTS.md` | 本文档，记录当前项目的协作与治理规则 |

## 查阅入口

| 文档总入口 | `README.md` | 仓库导航与当前文档索引 |
| 产品需求 | `docs/prd/2026-03-24-linpo-v0.6-observer-prd.md` | 当前有效的产品边界与当前阶段范围 |
| 当前架构 | `docs/architecture/2026-03-24-observer-architecture.md` | 当前有效架构边界与 v0.6 IA/验收约束入口 |
| 当前唯一计划 | `.sisyphus/plans/v0.6-backend-layered-breakly-implementation-work-plan.md` | 当前阶段唯一 active plan，用于定义与承载 v0.6 后端分层与 breakly 迁移目标、范围、验证与收口 |


## 开发环境与验证约定
- 当前开发环境为混合部署：前端/后端运行在宿主机进程，PostgreSQL 运行在 Docker 容器。
- PostgreSQL 对宿主机当前暴露端口为 `40193`。
- Python 测试默认通过仓库内虚拟环境执行：`/data/projects/linpo/.venv/bin/pytest`。
- 不保证系统全局 `pytest` 可用；执行后端测试时应显式使用 `.venv/bin/pytest`，避免 PATH 差异导致误判。
- 运行测试命令前，优先以仓库实际文件为准校验路径是否存在；计划文档中的历史测试文件名若与当前仓库不一致，应先修正文档口径再执行验证。
- 浏览器层验收优先从 `ravin` 发起 Playwright，对已部署端点执行端到端验证（前端 `http://175.178.213.10:5173`，后端 `http://175.178.213.10:8000`）。`ravin` 的角色是远程客户端与浏览器验收发起端，用于以用户视角访问已部署服务。
- `ravin` 不承载仓库代码执行，不作为构建、测试、部署主机，也不要求在 `ravin` 克隆仓库；仓库命令、构建与本地测试仍以当前开发工作区和对应运行环境为准。

## 所有权
- Lead: @platform

## 语言
- assistant 与 user 的协作沟通默认使用中文。
- 项目文档默认使用中文编写与维护；仅在外部标准明确要求 English 时例外。

## 反模式
- ❌ 在 `AGENTS.md` 记录历史流水账/变更日志。
- ❌ 将仓库外本地归档内容视为当前项目权威来源。
- ❌ 自己做所有任务，不委派 subagents。
- ❌ 在产品方向未冻结前提前铺设大而全的平台实现。
- ❌ 在 `docs/` 下重新引入 `README.md` / `AGENTS.md` / `CONSTITUTION.md` 一类规则或导航壳文档。
- ❌ 在未更新文档共识前直接开工实现复杂模块。

## 治理规则

### Doc-First 协作原则
- 当前阶段以 `README.md`、`docs/prd/`、`docs/architecture/` 中的当前有效文档作为 truth docs；当前唯一 active plan 为 `.sisyphus/plans/v0.6-backend-layered-breakly-implementation-work-plan.md`，用于承载当前阶段执行细节与收口要求。
- 口头约定无效；只有写入当前有效文档的约束才算数。
- 架构、产品边界或阶段目标发生变化时，必须先更新文档，再推进实现。
- “实现已改但文档未更新” = 项目漂移，必须立即修复。

### 关键决策升级与治理固化

**触发域**：
- 产品方向变更（目标用户、闭环场景、阶段路线）
- 架构方向变更（运行边界、外部系统接入、状态模型）
- 契约破坏性变更（接口、协议、错误码、鉴权机制）
- 部署拓扑变更（环境隔离、外部依赖、端口/主机约束）
- 所有权重新分配（目录职责、跨模块责任转移）

**触发条件**（满足任一即触发升级）：
- 变更影响超过一个目录或模块
- 变更破坏现有文档约束且无向后兼容方案
- 变更引入新的外部依赖或运行约束
- 对规范理解存在歧义且无文档先例
- 涉及当前文档未覆盖的新模式或边界情况

**动作链**：
1. **暂停实施**：立即停止相关代码修改
2. **文档先行**：先更新 `docs/` 中对应文档
3. **询问用户**：在 Session 中明确影响范围、备选方案与建议路径
4. **落盘决策**：用户确认后再继续推进
5. **实现跟进**：实现仅跟随已落盘文档推进

### 文档落盘边界
- 产品方向与范围：写入 `docs/prd/`
- 架构与模块边界：写入 `docs/architecture/`
- **长远规划 / 非当前阶段版本设计**：写入 `docs/plans/`
- **当前阶段唯一 active plan**：写入 `.sisyphus/plans/`
- `AGENTS.md` 仅固化重复适用的治理规则，不记录阶段性历史

### 目录协作规则
- 当前仓库处于重建早期，优先保持目录最小化。
- 未在文档中明确需要的目录，不应预先创建。
- 涉及新目录、新模块或新技术栈时，先更新相应文档，再创建文件结构。

### Subagent 优先协作规则
- 默认优先将可拆分任务委派给 subagents 并行推进；主 agent 负责目标拆解、顺序控制、结果校验与集成协调。
- 除非任务足够小且委派成本明显高于直接处理，否则主 agent 不应亲自承担具体实现。
- 需要跨模块、跨层级或需要外部资料调研的任务，应先拆分为可验证的子任务并委派执行。
- 主 agent 在采用 subagent 产出前，必须对结果进行核对，确保其符合当前 `docs/` 中已冻结的约束。
- 若 subagent 产出与文档共识冲突，应以文档共识为准，先更新文档或调整方案，再决定是否继续实现。

### 验证与验收规则
- 浏览器级验收以部署环境为准，优先使用 `ravin` 上的 Playwright 对已部署前后端进行联调验证。`ravin` 仅承担远程访问与浏览器验收入口，不承担仓库运行、构建、测试或部署职责。
- 本地组件测试可作为快速反馈，但不能替代浏览器级端到端验收结论。

### 版本与发布策略
- 当前阶段不强调语义化版本。
- 文档与阶段计划优先使用日期命名（YYYY-MM-DD）。
- 允许破坏性重做，但必须先在文档中说明原因与新方向。
- git commit 信息默认使用中文；若需使用 English，必须有明确外部约束或用户要求。
