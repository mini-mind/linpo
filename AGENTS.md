# Linpo 治理与协作规则（唯一真源）

本文件是项目治理规则唯一真源；`docs/` 不再维护独立治理正文。

## 1) 定位与语言

- 定位：Linpo 是 OpenClaw 与人之间的人机协作编排交互层。
- v0.7 主工作区：`摘要 + 看板 + 流程列表 + 流程编辑 + 文件`（不再维护旧 IA）。
- 所有权：Lead `@platform`。
- 默认中文协作与文档；外部标准要求时可用英文。

## 2) Doc-First（先文档后实现）

- 口头约定无效，只有仓库文档有效。
- 涉及产品/架构/契约/部署边界变更，先改文档再改实现。
- 发现实现与文档漂移，先修文档一致性再继续开发。
- 命中以下任一情况必须升级：方向变更、破坏性契约、部署拓扑变化、跨模块影响、引入新依赖/运行约束、文档歧义或未覆盖边界。
- 升级流程：暂停实施 → 更新文档草案 → 与用户确认影响与方案 → 确认后实施与验证。

## 3) 协作与执行

- 非微小任务先拆分子任务（边界/输入输出/验收标准）。
- 默认优先委派子代理，主 agent 负责调度、核验、集成。
- 默认 subagent 模型：`gpt-5.3-codex`（用户明确指定时例外）。
- 对 subagent 保持耐心，非卡死/超时不频繁中断。
- 极小且高耦合任务可直接处理。
- 并行产出与文档冲突时，以文档约束为准。

## 4) 工程准则

- BUG/疑难问题默认红绿测试（先红后绿）。
- 简单小改动可不新增测试，但必须做最小验证并说明。
- 失败处理默认“可回滚即回滚、错误默认暴露”；仅业务明确要求时才做降级兜底。

## 5) 提交边界（默认禁止）

- 未经用户明确提出“commit/提交”，禁止执行 `git commit`。
- `git push`、打 tag、改写历史（如 `rebase`/`reset --hard`）同样默认禁止。
- 需要阶段性保存时，保留工作区改动并汇报，不以隐式提交替代沟通。

## 6) 验证与验收

- 后端核心逻辑变更必须补单元测试。
- BUG/疑难问题需提供红绿测试证据。
- 后端测试命令：`/data/projects/linpo/.venv/bin/pytest`。
- 浏览器验收由 `ravin` 发起 Playwright（`ravin` 非构建/部署主机）。
- 每个 major feature 完成后先部署并验收，不延后到最后集中联调。

## 7) 目录规则

- 保持目录最小化；无明确需求不预建目录。
- 新目录/新模块/新技术栈：先更新文档再落地。
- 同一约束只在一个文档定义，避免并行口径。
- 过时文档直接删除，不保留废弃占位。
