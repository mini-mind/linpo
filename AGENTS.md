# Linpo 治理与协作规则（唯一真源）

本文件是项目治理规则的唯一真源。`docs/` 中不再维护独立治理正文。

## 1. 项目定位

Linpo 当前定位：位于 OpenClaw 与人之间的人机协作编排交互层。  
Linpo v0.7 主工作区为：`摘要 + 看板 + 流程列表 + 流程编辑 + 文件`，不再维护旧版 `overview / topology / team / session` 主 IA。

## 2. 所有权与语言

- Lead：`@platform`
- assistant 与 user 默认中文协作。
- 项目文档默认中文，外部标准要求时可使用英文。

## 3. Doc-First 与决策升级

- 口头约定无效，只有仓库文档有效。
- 产品、架构、契约、部署边界变更时，必须先更新文档，再推进实现。
- 发现实现与文档不一致，优先修复漂移再继续开发。

### 3.1 升级触发

以下任一条件命中即升级：

- 产品方向变更
- 架构方向变更
- 契约破坏性变更
- 部署拓扑变更
- 所有权或目录职责重分配
- 影响超过一个模块或目录
- 破坏现有约束且无兼容方案
- 引入新的外部依赖或运行约束
- 文档存在歧义且无先例
- 出现文档未覆盖的新边界情况

### 3.2 升级流程

1. 暂停实施。
2. 先更新文档草案。
3. 与用户确认影响范围、备选方案与建议路径。
4. 确认后再实施与验证。

## 4. 文档边界与读取策略

### 4.1 文档边界

- `README.md`：文档目录入口
- `docs/prd.md`：产品范围、交互主线、路由与成功标准
- `docs/architecture.md`：架构边界、分层、契约与迁移纪律
- `docs/test-resources.md`：测试/联调资源与 OpenClaw 参考
- `docs/openclaw-api-catalog.md`：OpenClaw 全量接口清单（含当前接入状态与未接入项）
- `docs/changelog.md`：文档结构与治理口径变更记录

### 4.2 按需读取（不全读）

- 默认先读：`README.md`。
- 功能/交互变更：读 `docs/prd.md`。
- 技术实现/边界变更：读 `docs/architecture.md`。
- 测试、部署、联调或外部协议细节：读 `docs/test-resources.md`。
- 需要查未接入接口或当前接入状态：读 `docs/openclaw-api-catalog.md`。

## 5. 需求拆分与协作

- 非微小任务默认先做需求拆分，再进入实现。
- 每个子任务需明确：边界、目标、输入输出、验收标准。
- 主 agent 负责拆解、顺序控制、结果核验与集成。
- 若并行产出与文档冲突，以文档约束为准。

## 5.1 执行方式

- 默认优先委派子代理完成可边界化、可并行的子任务，主 agent 负责调度、核验与集成。
- 默认所有 subagents 使用 `gpt-5.3-codex`；仅在用户明确指定其他模型时允许例外。
- 对 subagents 保持耐心：优先等待其自然完成与汇报，非明确卡死或超时异常，不得频繁中断、强停或反复催促。
- 对于极小且高耦合、委派成本明显高于直接处理的任务，主 agent 可直接执行。
- 子代理产出如与文档约束冲突，以文档约束为准并由主 agent 统一收敛。

## 5.2 工作准则

- 修改 BUG 或解决疑难问题时，默认采用红绿测试（先红后绿）流程。
- 简单小改动（如文案、样式、非核心配置）可不新增测试脚本，但必须完成最小化验证并在结果中说明。
- 各类失败默认遵循“可回滚即回滚、错误默认暴露”原则；仅在明确业务需求要求降级隐藏时才返回兜底结果。

## 5.3 提交边界（默认禁止）

- 在用户**未明确提出“commit/提交”**前，assistant 默认**禁止执行 `git commit`**。
- 同理，`git push`、打 tag、改写历史（rebase/reset --hard 等）也默认禁止，除非用户明确要求。
- 需要保存阶段性结果时，优先保持工作区改动并向用户汇报，不以隐式提交代替沟通。

## 6. 目录协作规则

- 保持目录最小化，不预建无明确需求目录。
- 涉及新目录、新模块、新技术栈时，先更新文档再创建结构。
- 同一约束只在一个文档中定义，避免并行口径。
- 过时文档直接删除，不保留废弃占位文件。

## 7. 验证与验收规则

- 后端核心逻辑变更必须有单元测试。
- 修改 BUG 或解决疑难问题时，需提供红绿测试证据（失败到通过）。
- 简单小改动可不新增测试脚本，但需执行与改动匹配的最小验证。
- 后端测试统一命令：`/data/projects/linpo/.venv/bin/pytest`。
- 浏览器验收以部署环境为准，由 `ravin` 发起 Playwright。
- `ravin` 仅承担远端访问与验收入口，不承担构建、测试、部署主机职责。
- 每个 major feature 完成后必须先部署并验收，不延后到末尾一次性联调。

## 8. 反模式

- 把仓库外归档当作当前权威来源。
- 在未更新文档共识前直接实现复杂模块。
- 同一规则在多个文档重复维护。
- 保留已完成且已失效的计划/废弃文档。
